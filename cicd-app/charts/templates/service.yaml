apiVersion: v1
kind: Service
metadata:
  name: {{ include "cicd-app.fullname" . }}
  labels:
    {{- include "cicd-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "cicd-app.selectorLabels" . | nindent 4 }}
